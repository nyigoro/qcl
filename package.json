{
  "name": "qcl",
  "version": "1.0.0",
  "description": "Quick Component Language: A universal, component-based markup language",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "qcl": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && npx pegjs --format commonjs src/qcl.pegjs -o src/parser.js",
    "test": "jest",
    "prepublishOnly": "npm run build",
    "deploy-docs": "cd docs && npm run build",
    "deploy-gh-pages": "gh-pages -d dist"
  },
  "dependencies": {
    "commander": "^9.4.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "esbuild": "^0.19.5",
    "gh-pages": "^6.0.0",
    "jest": "^29.7.0",
    "pegjs": "^0.10.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/quick-component-language.git"
  }
}
